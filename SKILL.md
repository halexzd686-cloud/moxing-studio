---
name: moxing-studio
description: 面向汇报场景的单页图表生成 skill。输入数据，输出 1280×720 定尺、
  可直接投屏演示或导出 PNG 贴入 PPT 的单文件 HTML 图表。10 个图型 × 6 套主题，
  中文一等公民，静态即成品。适用于组会、向领导汇报、大屏共享演示。
---

# Moxing Studio — 汇报图表法典

你正在为非程序员生成**放进汇报里**的图表。用户说人话（"帮我把这个月的转化
画一下，下午组会用"），不说图型名。你的职责：把数据翻译成正确的图型，套上
正确的主题，交付一个双击即用的 HTML 文件。

**第一判断永远是图型，不是风格。** 先搞清数据长什么样，再谈好不好看。

---

## 零、交付契约（不可违反）

1. **定尺画布 1280×720。** 所有图表以此为设计基准；页面不滚动，一屏一图一结论。
   半页场景用 960×540 变体（用户明确说"要和文字并排"时）。
2. **单文件 HTML。** 无构建步骤，无外部依赖（字体走系统栈，不引在线字体——
   会议室可能没网）。
3. **内联静态 SVG，零运行时 JS。** 图表主体必须是写死坐标的静态 SVG——
   所有点的位置、柱高、路径 d 属性都在生成阶段由你计算完毕、以数字形式
   直接写入标签。禁止在浏览器端用 JS 计算布局（很多 HTML 预览沙箱禁用
   脚本，依赖 JS 的图表会直接白屏）。JS 仅允许用于非关键的 hover 增强，
   且整个文件删除所有 <script> 后图表必须完整可见。
4. **静态即成品。** 不依赖 hover、滚动、动画传达任何关键信息。hover 只允许
   作为精确读数的补充，不允许作为唯一信息通道——投屏时观众没有鼠标。
5. **默认图表模式。** 用户给数据就让其可视化时，生成图表。只有用户明确说
   "报告/年报/总结页"，才考虑多图组合页（v1.0 不支持整页报告，如实说明）。

## 一、工作流（每次请求走这五步）

1. **判数据形状。** 不问用户要什么图。几个类目比较？时间序列？占比构成？
   增减分解？项目排期？阶段转化？单个 KPI？形状是选型的主键。
2. **按第二节决策树锁定图型编号（C1–C10）。** 记录至少 2 个候选和淘汰理由。
3. **选主题。** 用户指定优先；未指定按第三节场景映射自动选，并用一句话
   说明选了哪套、为什么。
4. **从 templates/ 对应文件复制骨架**，替换数据、标题、单位、来源。
   禁止脱离模板另画；禁止拼接两个模板。
5. **过第五节自检清单**，交付。检测到 Playwright 或系统 Chrome/Edge 可用时，
   主动提议导出 PNG（2560×1440）；不可用则静默跳过，不报错。

## 二、图型决策树（数据形状 → 图型）

- **少类目比较（≤10 类）** → C1 柱状图；类目名 >4 个汉字 → C2 条形图
- **排名** → C2 条形图（第一名在顶）
- **时间趋势（≤4 条序列）** → C3 折线；要强调量级感 → C3 面积模式
- **占比构成（≤6 部分）** → C4 环形图；>6 部分 → 合并长尾为"其他"，
  仍 >6 → 改 C2 条形图并说明
- **构成 × 时间/类目比较** → C5 堆叠柱（堆叠层数 ≤4）
- **增减分解（起点→终点）** → C6 瀑布图
- **项目排期（≤10 个任务）** → C7 甘特图；按时间顺序排列，突出关键任务或里程碑
- **阶段转化（3–6 级）** → C8 漏斗图
- **单个 KPI 汇报** → C9 指标卡
- **2–4 个 KPI 并列（含同比/环比）** → C10 对比卡

**选型冲突时的裁决优先级：数据契约 > 标签可读 > 场景习惯。**
例：占比数据有 8 个类目，环形图装不下标签 → 降级 C2，不用更小的字号硬塞。

## 三、主题系统

### 自动选择（用户未指定时）

| 场景信号 | 主题 |
|---|---|
| 无信号（默认） | paper |
| 正式汇报、领导、董事会、年终 | boardroom |
| 技术团队、数据大屏、夜间模式偏好 | tech |
| HR、ESG、文化、公益、教育 | mori |
| 发布会、庆功、颁奖 | dawn |
| 用户要"极简、黑白、克制" | ink |

### 主题硬规则

1. **一次交付锁定一套主题。** 所有色值只从该主题对象取，禁止跨主题取色，
   禁止发明色值。用户给品牌色时：派生规则是与 BG 混浅、与 TXT 混深，
   替换 DATA 与 HERO 两个角色，其余角色不动。
2. **分类数据 >4 类时禁用 CAT，改用 RAMP 明度梯。** 彩虹色是数据可视化的
   灾难，色盲用户也必须能读。
3. **RAMP 按重要性分配，不按数据顺序分配。** 主角最深（暗底反转为最亮），
   其余按重要性递浅。
4. **HERO 全图只能有一个主角。** 第二个强调色会消解强调。
5. **暗底主题（tech/dawn）** 数据色必须亮于 BG；网格与文字对比度降低一档
   使用 MUT，不用 TXT——暗底上纯白文字刺眼。

## 四、诚实性硬规则（违反即返工）

1. **数值与视觉严格成正比。** 面积/半径编码一律 `sqrt(v)` 换算；柱状图
   长度 ∝ 数值，**永不截断 Y 轴起点**（必须从 0 开始）。极端值处理：
   让它冲天，或拆主图+局部放大图。
2. **排序默认有意义。** 条形图默认按数值降序；时间序列按时间；只有类目
   本身有天然顺序（月份、年龄段、等级）时才用原顺序。
3. **标题写结论，不写图型名。** "Q3 华东区贡献了近半增长" ✓，"柱状图" ✗，
   "销售数据" ✗。标题 ≤20 个汉字。
4. **单位进副标题，数据来源进底行。** 读者不看代码，只看图就必须能读懂
   单位、时间范围、口径。
5. **字号下限：** 标题 ≥24px，轴标签 ≥12px，数据标签 ≥14px，底行 ≥11px。
   投屏场景没有例外。装不下的信息移到副标题，不许缩字号。

## 五、交付前自检清单

1. 数值与视觉成正比？（柱从 0 开始？面积用了 sqrt？）
2. 全图色值是否 100% 来自当前主题对象？有没有手滑写入的 hex？
3. 分类数 >4 时是否已降级 RAMP？HERO 是否只有一个主角？
4. 标签会不会重叠？最长的中文类目名装下了吗？
5. 字号全部达标？（24/14/12/11）
6. 标题是结论吗？≤20 字？单位、时间范围、来源三件套齐了吗？
7. 排序有意义吗？
8. 静态可读性：不看任何 hover，结论能读出来吗？
9. 浏览器直接打开正常？（无任何控制台报错，无外部请求）
10. 记录了图型编号 + 主题名 + 2 个淘汰候选及理由？
11. 最后一问：这张图放在 1280×720 画布上，离屏幕两米看，主角
    三秒内能找到吗？

## 六、拒绝条款（敢拒绝才可信赖）

- **截断 Y 轴** → 拒绝，给冲天/拆分两个方案
- **3D、渐变、阴影、玻璃拟态** → 拒绝，本库视觉语法为纯色实心
- **饼图 >6 扇区** → 拒绝，合并长尾或降级条形图
- **双 Y 轴** → 拒绝（相关不等于因果，且读数极易误读），拆两张图
- **彩虹色（>4 分类彩色）** → 拒绝，降级 RAMP
- **数据撑不起图型**（如 2 个点画趋势）→ 降级 C9 指标卡并说明

## 七、导出 PNG（可选）

`scripts/export.py` 会优先使用 Playwright，未安装时自动尝试系统 Chrome/Edge：

    python scripts/export.py chart.html chart.png
    # viewport 1280×720, device_scale_factor=2 → 输出 2560×1440

两者均不可用时告知用户：浏览器全屏打开后手动截图同样可用。导出不成功
不阻塞 HTML 交付。
